# Update Log

## 2026-08-17

* **Init**: Criada a estrutura do bundle OKF em `docs/okf`.
* **Create**: Estabelecidas as 4 capacidades em `/domain` (distribuicao, indicacao, extrato, auth).
* **Create**: Documentadas as 6 entidades em `/entities` (CashbackConsumer, Consumer, CashbackTransaction, CashbackRates, Payable, Payload).
* **Create**: Documentados os 8 endpoints em `/endpoints`.
* **Create**: Extraidas 5 regras de negocio transversais em `/rules`.
* **Create**: Documentados os 5 use cases, 7 repositorios, 1 service e o SupabaseClient em `/components`.
* **Create**: Documentados os 2 providers (Supabase, Mensageria Produce) em `/providers`.
* **Create**: Criada skill `okf-update` em `.devin/skills/okf-update/SKILL.md` para automatizar futuras sincronizacoes.
